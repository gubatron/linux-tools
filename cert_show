#!/usr/bin/env bash
if [ ! -d "certbot.d" ]; then
	echo ""
	echo "Error: No certbot.d folder has been created to store your LetsEncrypt account config and certs in this folder"
  echo ""
  exit 1
fi

certbot certificates --work-dir certbot.d --config-dir certbot.d --logs-dir certbot.d
